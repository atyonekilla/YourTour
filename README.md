# YourTour

**YourTour** is a fully responsive static landing page designed for discovering and requesting travel tours.  
The project focuses on clean semantic markup, structured styling, and precise adherence to the original Figma layout across mobile, tablet, and desktop breakpoints.

---


---
####
- 📱 **Responsive design** optimized for 360 px, 1024 px, and 1920 px screen widths  
- 📌 **Sticky navigation bar** that remains visible during page scroll  
- 📝 **Tour request form** with built-in validation for email, phone number, dates, and selection fields  

---

## 🛠  Stack

- **HTML5** — semantic and accessible markup  
- **CSS** — modular adaptive styles (mobile / tablet / desktop)  
- **Vanilla JavaScript (ES6+)** — interactive elements and UI behavior  
- **No external build tools** — the project is implemented as a fully static website  

---

## 🚀 Getting Started

The project has no external dependencies and requires no build steps.

```bash
# Clone the repository
git clone https://github.com/atyonekilla/YourTour.git

# Navigate to the project directory
cd YourTour

# Open the application
# Windows: open index.html via File Explorer
# macOS/Linux: use the file manager or run:
open index.html
